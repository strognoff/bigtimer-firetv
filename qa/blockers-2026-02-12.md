# Blockers Log — 2026-02-12

## Current blocker
- Missing system `gradle` binary on build host.

## Verification evidence
Command:
```bash
cd android-app
gradle -v
```
Result:
```text
bash: gradle: command not found
```

## Impact
- Cannot generate Gradle wrapper (`gradle wrapper --gradle-version 8.7`)
- Cannot run first `./gradlew assembleDebug`

## Unblocked path
1. Install Gradle (or use Android Studio embedded Gradle)
2. Run `make bootstrap`
3. Commit generated wrapper + first successful build note
